"use client";

export default function LidarPopup({ ros }) {

  return (
    <div style={{ padding: 10 }}>
      <p>Live Lidar Stream (debug mode)</p>
      <p style={{ color: "#888" }}>Console will show ranges…</p>
    </div>
  );
}
